Create a credentials.ini file in the directory and create a variable called [API_KEY] under a  section called [gemini_app] and set it to the API KEY you have for the google gemini chatbot. It should look something like:
[gemini_app]
API_KEY = <your_key>